# ==============================================
# KTV 환경변수 설정 (Cloudflare HTTPS용)
# ==============================================

# 서버 포트
PORT=13579

# 데이터베이스 설정
DB_HOST=db
DB_USER=ktv_user
DB_PASSWORD=ktv_password
DB_NAME=ktv_db
DB_PORT=3307

# Kakao 로그인 설정
KAKAO_CLIENT_ID=your_kakao_client_id
KAKAO_CLIENT_SECRET=your_kakao_client_secret

# ⚠️ 중요: HTTPS로 변경 (Cloudflare 사용)
REDIRECT_URI=https://your-domain.com/api/auth/kakao/callback

# 초대 코드
INVITE_CODE=your_invite_code

# 관리자 코드
ADMIN_CODE=your_admin_code

# JWT 시크릿 (최소 32자 이상 권장)
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long

# ==============================================
# 주의사항:
# 1. your-domain.com을 실제 도메인으로 변경하세요
# 2. 모든 비밀번호와 시크릿은 강력한 값으로 변경하세요
# 3. 이 파일은 절대 Git에 커밋하지 마세요 (.gitignore 확인)
# ==============================================
